# SSH - never commit keys
.ssh/id_*
.ssh/known_hosts
.ssh/authorized_keys

# Machine/work specific overrides, never committed
**/*.local
**/*.local.*

# Sensitive/private files, never committed
**/*.private
**/*.private.*

# macOS
.DS_Store

# Dotfiles repo itself
.dotfiles/
